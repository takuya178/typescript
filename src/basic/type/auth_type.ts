export type quizType = {
  quizText: string,
  quizImageList: object[]
}[]

export type Image = {
  img: HTMLImageElement,
  judge: boolean
}

export type imgSrcAndBoolList = {
  imgSrc: string,
  judge: boolean
}